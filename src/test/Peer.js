'use strict';

import Peer from 'src/js/og.core/Peer';

var peer = new Peer();

describe('Peer', () => {
  it('should return "Do Something" when calling doSomething', () => {
    expect('c').to.equal('c');
  });
});

